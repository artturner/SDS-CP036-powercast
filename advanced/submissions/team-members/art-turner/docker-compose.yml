version: '3.8'

services:
  powercast-api:
    build: .
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - PYTHONPATH=/app
      - PORT=${PORT:-8000}
    volumes:
      - .:/app
    restart: unless-stopped
    # Healthcheck is defined in Dockerfile using Python stdlib
